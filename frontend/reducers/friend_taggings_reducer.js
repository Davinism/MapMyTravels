import { FriendTaggingConstants } from '../actions/friend_tagging_actions';
import merge from 'lodash/merge';

const FriendTaggingsReducer = function(state = {}, action) {
  switch (action.type) {

    default:
      return state;
  }
};

export default FriendTaggingsReducer;
